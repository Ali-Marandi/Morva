name: M3.44 Independent Evidence Verifier

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  independent-verifier:
    name: Rebuild evidence chain and verify signature
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e '.[dev]'
      - run: python -m pip install build
      - run: >
          ruff check
          src/morva/runtime/release_evidence.py
          src/morva/runtime/release_gate.py
          src/morva/runtime/release_manifest.py
          src/morva/runtime/release_rehearsal.py
          tools/m3_40_release_gate.py
          tools/m3_41_release_manifest.py
          tools/m3_42_release_rehearsal.py
          tools/m3_43_release_evidence.py
          tools/m3_44_evidence_verifier.py
          tests/test_evidence_verifier_m3_44.py
          src/morva/runtime/signed_trusted_key_registry.py
          src/morva/runtime/trusted_key_registry.py
          tools/m3_46_signed_trusted_registry.py
          tests/test_signed_trusted_key_registry_m3_46.py
          tests/test_signed_trusted_registry_serialization_m3_46.py
      - run: >
          pytest -q
          tests/test_release_manifest_m3_41.py
          tests/test_release_rehearsal_m3_42.py
          tests/test_release_evidence_m3_43.py
          tests/test_evidence_verifier_m3_44.py
          tests/test_signed_trusted_key_registry_m3_46.py
      - name: Build distributions
        run: rm -rf dist && python -m build --outdir dist
      - name: Resolve release tag
        run: |
          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            echo "RELEASE_TAG=${GITHUB_REF_NAME}" >> "${GITHUB_ENV}"
          else
            echo "RELEASE_TAG=ci-${GITHUB_SHA}" >> "${GITHUB_ENV}"
          fi
      - name: Generate manifest
        run: >
          python -m tools.m3_41_release_manifest dist
          --release-id "morva-${RELEASE_TAG}"
          --tag "${RELEASE_TAG}"
          --candidate-sha "${GITHUB_SHA}"
          --output release-evidence-manifest.json
      - name: Verify manifest
        run: >
          python -m tools.m3_41_release_manifest dist
          --candidate-sha "${GITHUB_SHA}"
          --verify release-evidence-manifest.json
      - name: Create blocked rehearsal gate fixture
        run: |
          python - <<'PY'
          import json
          from datetime import datetime, timezone
          from pathlib import Path

          from morva.runtime.release_attestation import ArtifactAttestation, ReleaseAttestation
          from morva.runtime.release_certification import ReleaseCertification
          from morva.runtime.release_gate import ReleaseGate
          from morva.runtime.security_assessment import SecurityAssessment
          from tools.m3_41_release_manifest import load_manifest

          manifest = load_manifest(Path("release-evidence-manifest.json"))
          now = datetime(2026, 1, 1, tzinfo=timezone.utc)
          security = SecurityAssessment(
              assessment_id="CI-REHEARSAL-M3-44",
              assessed_at=now,
              scope_hash="0" * 64,
              required_controls=(
                  "authentication",
                  "authorization",
                  "cryptography",
                  "auditability",
              ),
              verified_controls=(),
          )
          certification = ReleaseCertification(
              release_id=manifest.release_id,
              candidate_sha=manifest.candidate_sha,
              required_evidence=(
                  "security-report",
                  "dr-evidence",
                  "load-evidence",
                  "reconciliation-evidence",
              ),
              verified_evidence=(),
          )
          attestation = ReleaseAttestation(
              release_id=manifest.release_id,
              tag=manifest.tag,
              candidate_sha=manifest.candidate_sha,
              certification_fingerprint=certification.fingerprint,
              evidence_bundle_fingerprint="1" * 64,
              artifacts=tuple(
                  ArtifactAttestation(item.path, item.sha256)
                  for item in manifest.artifacts
              ),
          )
          gate = ReleaseGate(manifest.candidate_sha, security, certification, attestation)
          if gate.release_ready:
              raise SystemExit("CI rehearsal gate unexpectedly became ready")
          payload = {
              "rehearsal_only": True,
              "candidate_sha": gate.candidate_sha,
              "security_assessment": {
                  "assessment_id": security.assessment_id,
                  "assessed_at": security.assessed_at.isoformat(),
                  "scope_hash": security.scope_hash,
                  "required_controls": list(security.required_controls),
                  "verified_controls": list(security.verified_controls),
                  "findings": [],
                  "independent_assessor": None,
                  "independent_report_uri": None,
                  "independent_signed_at": None,
              },
              "certification": {
                  "release_id": certification.release_id,
                  "candidate_sha": certification.candidate_sha,
                  "required_evidence": list(certification.required_evidence),
                  "verified_evidence": list(certification.verified_evidence),
                  "security_signoff_complete": False,
                  "disaster_recovery_signoff_complete": False,
                  "load_signoff_complete": False,
                  "reconciliation_signoff_complete": False,
                  "signoffs": [],
              },
              "attestation": {
                  "release_id": attestation.release_id,
                  "tag": attestation.tag,
                  "candidate_sha": attestation.candidate_sha,
                  "certification_fingerprint": attestation.certification_fingerprint,
                  "evidence_bundle_fingerprint": attestation.evidence_bundle_fingerprint,
                  "artifacts": [
                      {"path": item.path, "sha256": item.sha256}
                      for item in attestation.artifacts
                  ],
                  "signer": None,
                  "signed_at": None,
                  "signature_uri": None,
                  "release_uri": None,
              },
          }
          Path("release-gate-rehearsal.json").write_text(
              json.dumps(payload, ensure_ascii=True, sort_keys=True, indent=2) + "\n",
              encoding="utf-8",
          )
          PY
      - name: Create rehearsal summary
        run: >
          python -m tools.m3_42_release_rehearsal
          release-evidence-manifest.json
          release-gate-rehearsal.json
          --artifact-dir dist
          --expected-sha "${GITHUB_SHA}"
          --output release-rehearsal-evidence.json
      - name: Generate ephemeral signing key
        run: |
          python - <<'PY'
          from pathlib import Path
          from cryptography.hazmat.primitives import serialization
          from cryptography.hazmat.primitives.asymmetric.ed25519 import Ed25519PrivateKey

          key = Ed25519PrivateKey.generate()
          Path("ci-ed25519-private.pem").write_bytes(
              key.private_bytes(
                  serialization.Encoding.PEM,
                  serialization.PrivateFormat.PKCS8,
                  serialization.NoEncryption(),
              )
          )
          Path("ci-ed25519-public.pem").write_bytes(
              key.public_key().public_bytes(
                  serialization.Encoding.PEM,
                  serialization.PublicFormat.SubjectPublicKeyInfo,
              )
          )
          PY
          chmod 600 ci-ed25519-private.pem
      - name: Create and sign trusted signing-key registry
        run: |
          python - <<'PY'
          from datetime import datetime, timezone
          from pathlib import Path

          from cryptography.hazmat.primitives import serialization
          from cryptography.hazmat.primitives.asymmetric.ed25519 import Ed25519PrivateKey

          from morva.runtime.trusted_key_registry import TrustedKeyRegistry, TrustedSigningKey
          from morva.runtime.signed_trusted_key_registry import SignedTrustedKeyRegistry
          from tools.m3_46_signed_trusted_registry import write_signed_registry

          signing_public = serialization.load_pem_public_key(
              Path("ci-ed25519-public.pem").read_bytes()
          )
          registry = TrustedKeyRegistry(
              registry_id="ci-rehearsal-signing",
              version=1,
              keys=(
                  TrustedSigningKey(
                      key_id=TrustedKeyRegistry.key_id_for(signing_public),
                      public_key_sha256=TrustedKeyRegistry.public_key_sha256_for(signing_public),
                      status="active",
                      valid_from=datetime(2026, 1, 1, tzinfo=timezone.utc),
                  ),
              ),
          )
          root = Ed25519PrivateKey.generate()
          Path("ci-registry-root-private.pem").write_bytes(
              root.private_bytes(
                  serialization.Encoding.PEM,
                  serialization.PrivateFormat.PKCS8,
                  serialization.NoEncryption(),
              )
          )
          Path("ci-registry-root-public.pem").write_bytes(
              root.public_key().public_bytes(
                  serialization.Encoding.PEM,
                  serialization.PublicFormat.SubjectPublicKeyInfo,
              )
          )
          signed = SignedTrustedKeyRegistry(registry).sign(
              root, datetime(2026, 1, 1, tzinfo=timezone.utc)
          )
          write_signed_registry(signed, Path("trusted-key-registry.json"))
          PY
          chmod 600 ci-registry-root-private.pem

      - name: Sign evidence bundle
        run: >
          python -m tools.m3_43_release_evidence
          release-evidence-bundle.json
          --manifest release-evidence-manifest.json
          --gate release-gate-rehearsal.json
          --rehearsal release-rehearsal-evidence.json
          --registry trusted-key-registry.json
          --root .
          --private-key ci-ed25519-private.pem
          --output release-evidence-bundle.json
          --expected-sha "${GITHUB_SHA}"
      - name: Independently verify evidence chain
        run: >
          python -m tools.m3_44_evidence_verifier
          release-evidence-bundle.json
          --manifest release-evidence-manifest.json
          --gate release-gate-rehearsal.json
          --rehearsal release-rehearsal-evidence.json
          --public-key ci-ed25519-public.pem
          --registry trusted-key-registry.json
          --registry-root-public-key ci-registry-root-public.pem
          --root .
          --expected-sha "${GITHUB_SHA}"
      - name: Confirm private key is excluded
        run: |
          rm -f ci-ed25519-private.pem ci-registry-root-private.pem
          echo "Ephemeral private keys removed before artifact upload."
      - name: Upload verifier evidence
        uses: actions/upload-artifact@v4
        with:
          name: morva-m3-44-independent-verifier
          path: |
            dist/
            release-evidence-manifest.json
            release-gate-rehearsal.json
            release-rehearsal-evidence.json
            release-evidence-bundle.json
            ci-ed25519-public.pem
            trusted-key-registry.json
            ci-registry-root-public.pem
          if-no-files-found: error
